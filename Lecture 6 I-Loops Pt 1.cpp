#include<iostream>
using namespace std;
int main(){
	// Action should be decided in every loop
	// How many times the loop is going to run
	// types - for  while
	
	// Initialization - what happens at the start of loop
	int i; // will only take place once
	for(i=0;   ;){
		cout<<i<<" ";
	}
	return 0;
}
